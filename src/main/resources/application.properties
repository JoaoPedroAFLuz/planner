spring.application.name=timely

spring.datasource.url=jdbc:postgresql://localhost:5432/timely
spring.datasource.username=JoaoPedroAFLuz
spring.datasource.password=docker
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
api.security.token.access_secret=${ACCESS_TOKEN_SECRET:unsecure-access-secret}
api.security.token.refresh_secret=${REFRESH_TOKEN_SECRET:unsecure-refresh-secret}